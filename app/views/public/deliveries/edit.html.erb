<!DOCTYPE html>
<html>
  <head>
    <title>商品一覧</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
     <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag 'deliveries', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  
  <body>
    <div class="container">
      <div class="row">
        <div class="col-sm-12">
          <h4 class="col-md-3 offset-1 bg-light text-center mt-2">配送先編集</h4>
    
          <% if @delivery.errors.any? %>
            <%= @delivery.errors.count %>error prohibited this obj from being saved:
            <% @delivery.errors.full_messages.each do |message| %>
              <%= message %>
            <% end %>
          <% end %>
    
    
          <%= form_with model:@delivery,local:true do |f| %>
    
            <div class="form-group">
              <label for ="delivery_postcode">郵便番号（ハイフンなし）</label>
              <%= f.text_field :postcode, autofocus: true, class: "form-control", id:"inpuPostcode", placeholder:"タイトル"%>
            </div>
    
            <div class="form-group">
              <label for ="delivery_address">住所</label>
              <%= f.text_area :address, class: "form-control", id:"inputAddress"%>
            </div>
    
            <div class="form-group">
              <label for ="delivery_name">宛名</label>
              <%= f.text_area :name, class: "form-control", id:"inputName"%>
            </div>
    
            <%= f.submit "変更内容を保存", class: "btn btn-success" %>
          <% end %>
        </div>
      </div>
    </div>
  </body>
</html>